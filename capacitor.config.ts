import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'help-me-remember',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
